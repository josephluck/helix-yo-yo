{
  "name": "helix-basic",
  "version": "1.0.0",
  "description": "Basic example of helix",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "budo ./src/index.ts --live --pushstate --host=localhost --port=8888 --base=/ -- -p [ tsify ]",
    "dist": "tsc && npm run browserify",
    "browserify": "browserify -g uglifyify dist/index.js -o dist/bundle.min.js"
  },
  "author": "Joseph Luck",
  "license": "ISC",
  "devDependencies": {
    "budo": "^9.4.7",
    "tsify": "^3.0.1",
    "tslint": "^4.4.2",
    "typescript": "^2.2.1",
    "@types/node": "^7.0.5",
    "browserify": "^14.1.0",
    "tslint-config-standard": "^4.0.0",
    "uglifyify": "^3.0.4",
    "uglifyjs": "^2.4.10"
  }
}
